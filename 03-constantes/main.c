#include <stdio.h>

int main(int argc, char const *argv[]) {
    
    // Las constantes simplemente son valores que no pueden ser modificacos durante la ejecucion del programa, su sintaxis es la siguiente

    const float PI = 3.141592;

    printf("%f", PI);

    return 0;
}
